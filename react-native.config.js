/** Ensures RN autolinking uses the same Android package as app.json / native project. */
module.exports = {
  project: {
    android: {
      packageName: 'com.dev.primotech.freshcart',
    },
  },
};
